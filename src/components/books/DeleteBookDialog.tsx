"use client";
import { AlertTriangle, Loader2, X } from "lucide-react";
import { useState } from "react";
import { deleteBookAction } from "@/actions/bookActions";
import type { Book } from "@/types/book";
export default function DeleteBookDialog({ book, onClose, onDeleted }: { book: Book; onClose: () => void; onDeleted: () => void }) { const [error, setError] = useState(""); const [deleting, setDeleting] = useState(false); async function remove() { setDeleting(true); const result = await deleteBookAction(book.id); setDeleting(false); if (!result.success) { setError(result.error || "Unable to delete book."); return; } onDeleted(); } return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-rose-50 text-rose-600"><AlertTriangle size={21} /></div><button type="button" onClick={onClose} title="Close" className="text-slate-400"><X size={19} /></button></div><h2 className="mt-5 text-lg font-semibold text-slate-900">Delete this book?</h2><p className="mt-2 text-sm leading-6 text-slate-500">This will permanently remove <strong className="text-slate-700">{book.title}</strong> from the catalog. Books with active loans cannot be deleted.</p>{error && <p className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl px-4 py-2.5 text-sm text-slate-600 hover:bg-slate-100">Cancel</button><button type="button" disabled={deleting} onClick={remove} className="inline-flex items-center gap-2 rounded-xl bg-rose-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-rose-700 disabled:opacity-60">{deleting && <Loader2 size={16} className="animate-spin" />}Delete book</button></div></div></div>; }
