import { Eye, Pencil, Trash2 } from "lucide-react";
import AvailabilityBadge from "./AvailabilityBadge";
import type { Book } from "@/types/book";

export default function BookTable({ books, onView, onEdit, onDelete }: { books: Book[]; onView: (book: Book) => void; onEdit: (book: Book) => void; onDelete: (book: Book) => void }) {
  return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Book</th><th className="px-5 py-4">Category</th><th className="px-5 py-4">Shelf</th><th className="px-5 py-4">Availability</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{books.map((book) => <tr key={book.id} className="transition hover:bg-slate-50/70"><td className="px-5 py-4"><button type="button" onClick={() => onView(book)} className="text-left"><p className="font-semibold text-slate-900 hover:text-blue-600">{book.title}</p><p className="mt-1 text-xs text-slate-500">{book.author} · {book.isbn}</p></button></td><td className="px-5 py-4 text-slate-600">{book.category}</td><td className="px-5 py-4 text-slate-600">{book.shelfLocation || "-"}</td><td className="px-5 py-4"><AvailabilityBadge availableCopies={book.availableCopies} totalCopies={book.totalCopies} /></td><td className="px-5 py-4"><div className="flex justify-end gap-1"><button type="button" title="View details" onClick={() => onView(book)} className="rounded-lg p-2 text-slate-400 hover:bg-blue-50 hover:text-blue-600"><Eye size={16} /></button><button type="button" title="Edit book" onClick={() => onEdit(book)} className="rounded-lg p-2 text-slate-400 hover:bg-amber-50 hover:text-amber-600"><Pencil size={16} /></button><button type="button" title="Delete book" onClick={() => onDelete(book)} className="rounded-lg p-2 text-slate-400 hover:bg-rose-50 hover:text-rose-600"><Trash2 size={16} /></button></div></td></tr>)}</tbody></table></div>;
}
